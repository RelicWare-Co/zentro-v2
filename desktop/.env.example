# URL que Electron carga dentro de la ventana desktop.
# En desarrollo puedes dejarlo vacío: la app usa http://localhost:3000.
# Antes de ejecutar `bun run desktop:make`, define aquí la URL pública real.
ZENTRO_DESKTOP_WEB_URL="https://app.example.com"
